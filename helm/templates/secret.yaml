apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{ .Values.secret.key }}: {{ .Values.secret.tokenKey | b64enc | quote }}
